# 2026-09-23 20:08 JST 本番サーバー通知の調査

## 確認できた事実

- 20:08:46–47 JST に同じ汎用文面の通知が短時間に複数届いた。
- 該当時間帯の本番リクエストログには `GET /api/persona/images/:id` が15件あり、すべて HTTP 200。うち11件に `[runtime-alert] delivery` の記録がある。通知は9つの実行インスタンスに分散していた。
- 同時間帯の関連するペルソナAPIも HTTP 200 または 204。確認した範囲では関数の失敗・画像取得失敗は見つからなかった。
- Vercel のエラーレベルログは0件。通知記録に元の `console.error` の引数や呼び出し元が含まれず、発火原因は特定できていない。汎用文面だけで利用者やデータへの影響がないとは断定できない。

## 対処

- `console.error` 通知に安全な照合指紋を付け、既存の `SystemSetting` を使う発生箇所単位の原子的な10分間の共有上限を追加。実行インスタンスをまたいだ通知の多重送信を抑え、DBの上限キーがエラー本文ごとに増えないようにする。
- スタックから呼び出し元が取得できない場合は従来と同じ発生箇所単位でまとめる。通知が失敗した場合は、その通知が取得した共有上限だけを解放する。DBの共有上限が使えない場合も通知自体は継続する。
- 通知と安全な診断ログには照合指紋を記録し、エラー本文・画像ID・リクエスト内容は含めない。

## 検証と残る確認

- オフライン回帰テストでは、30インスタンスの同時取得で1件だけが上限を取得し、11インスタンスからの汎用エラー通知も1件に収まった。
- 実際のPostgreSQLで無効な既存値、競合、期限内の再取得を確認した。
- 元の `console.error` の原因はログに残っていないため未解明。次に同じ通知が出た場合は、照合指紋、デプロイID、同時刻の関数ログ、利用者操作と画像表示を確認する。復旧確認までは「解消済み」と案内しない。

## 20:42 JST の公開後確認

- 本番デプロイ `dpl_3C6vPGcLeAyNxg9pmWLm9PLjJ7Mc` が Ready となり、公開URLが切り替わった。トップとペルソナ画面は HTTP 200。
- 調査用に非ログインで画像APIへ1回アクセスしたところ、期待どおり HTTP 401 だったが、同じリクエストで通知が1件発生した（20:42:35 JST、照合ID `5a4bb737-36b0-4427-a2b1-ca4767bf76c3`、指紋 `ef4886c13c74d60de2017d0b`）。この検査が通知を誘発した可能性がある。検査後は同じ本番リクエストを繰り返していない。
- 同じ操作を `VERCEL_ENV=preview` のローカル起動で1回試した場合は HTTP 401 で、`console.error` は再現しなかった。環境差または本番の別の処理が残るため、通知の元になった `console.error` の原因は依然として未特定。
- 公開直後の限定したログ窓でエラーレベルのログは0件。ただし上記の通知が1件あり、「エラーが完全に消えた」とは判断しない。
- 20:42:40–20:47:01 JST のログには追加の `[runtime-alert]` はなかった。共有上限の失敗警告も20:42の検査窓にはなかった。ローカルから本番DBの上限行を直接読む試みは Prisma の接続初期化エラーで失敗したため、実DB行の読取検証は未完了。

## 21:00 JST の追加調査

- `/api/cron/persona-purge` でも21:00:40 JSTに通知が1件発生した。照合指紋 `ef4886c13c74d60de2017d0b` は20:42の非ログイン画像API検査と同じで、引数1個・Error型なし・発生箇所不明という特徴も一致する。
- VercelのHTTPステータスフィルターでは、このCronはHTTP 200に含まれ、同時間帯の5xxは0件だった。画像API固有の障害とは言えず、成功した処理中にも同じ `console.error` が出ている。元の引数はログから復元できないため、原因の特定や無害判定は保留する。
- 次回の通知記録に、本文を含めず引数の種類と固定語彙のエラー系統を追加した。これで同じ指紋の通知が再発した際に切り分けを進める。実際の系統は次の自然発生ログで確認する。

## 21:30・21:45 JST の自然発生ログ

- 21:30:40、21:45:40にも `/api/cron/persona-purge` がHTTP 200で完了し、同じ指紋 `ef4886c13c74d60de2017d0b` の通知が各1件発生した。定期処理の実行間隔と一致するが、処理内部のどの段階で発生したかはまだ分からない。
- 追加した診断項目では引数は**文字列1件**、系統は `unclassified`、Error型はなし。原文・スタックは通知ログに保存していない。`persona-purge` のルートと画像削除処理に直接の `console.error` はなく、依存ライブラリや共通処理を含む発生源の特定は継続する。
- HTTP 200は削除処理全件の完全成功を証明しない。HTTP 503なら `result.failed > 0` または外側例外だが、この2件はHTTP 200のため少なくともその状態ではない。対象画像数と利用者影響は未確認。

## 22:30 JST 以降の切り分け

- 22:30:40 JSTにも旧デプロイの `/api/cron/persona-purge` で同じ照合指紋 `ef4886c13c74d60de2017d0b` の通知が発生した。20:42:35の非ログイン画像API確認だけに固有の通知ではない。
- 通知器は従来、スタックの最初の候補行にファイル位置がなければ後続を見ずに「サーバー処理」としていた。後続8行まで静的なファイル名・行番号を探索し、診断ログに `stackState`（`missing` / `unparsed` / `parsed`）を追加した。原文、スタック全文、パス、ユーザー入力は通知へ載せない。
- これは原因特定のための診断補修であり、元の `console.error` の原因や利用者影響はまだ未確定。修正後の自然発生ログで呼び出し元を照合するまでは復旧と判断しない。
- 23:00:40 JSTの新デプロイ上の自然発生通知も `stackState: unparsed` だった。スタック文字列の有無だけでは形式を判別できないため、次の診断では行数、位置表記の有無、V8の `stackTraceLimit` を数値・真偽値のみで記録する。スタック原文は保存しない。

## 23:15 JST の自然発生ログ

- デプロイ `dpl_Ehf1z9v54iG63qxVvc5uqo1XhzzU` 上の `/api/cron/persona-purge` が HTTP 200 のまま、23:15:42 JST に同じ指紋の通知を送信した。
- `stackState: unparsed`、`stackLineCount: 7`、`stackHasLocation: true`、`stackTraceLimit: 10`。元の `console.error` は文字列1件で、Vercelのエラーレベルログには同時刻の詳細がない。スタック中の位置がアプリ由来か `node:internal` 由来かは未確定。
- クエリ文字列付きのバンドルファイル位置も安全なファイル名だけで解析できるようにし、次回は内部以外の位置情報とスクリプト拡張子の有無を真偽値で記録する。原因と利用者影響は引き続き未特定で、解消済みとは判断しない。

## 23:30 JST の自然発生ログ

- 新デプロイ `dpl_3p7ab5PpVirU2zPokoGForT7TAPr` でも `/api/cron/persona-purge` が HTTP 200 で、23:30:40 JST に同じ指紋の通知を送信した。
- `stackLineCount: 7`、`stackExternalLocation: true`、`stackScriptLocation: true` だったが、呼び出し元はまだ `unparsed`。内部以外の位置表記とスクリプト拡張子はスタック内にある。ただし両者が同一フレームかは現診断からは分からない。元のエラー原因・影響は未特定のまま。
- 安全なファイル名抽出を、位置番号の後に記号があるバンドル形式にも対応させた。次回は「内部以外のスクリプト位置」の有無を真偽値で記録する。原文・パスは保存しない。

## 23:45 JST の自然発生ログ

- `/api/cron/persona-purge` は HTTP 200 で、23:45:40 JST に同じ指紋の通知を送信した。`stackExternalLocation: true`、`stackScriptLocation: true` だが `stackExternalScriptLocation: false`。スタック内のスクリプト位置は通知器自身または内部フレームに限られ、元の呼び出し元をその文字列から抽出できない。
- リポジトリのソース中にある固定 `console.error` 第一引数640件を、ローカルの2つの `NEXTAUTH_SECRET` 設定で同じ指紋計算にかけたが一致しなかった。本番の秘密値との一致は未確認であり、依存ライブラリ起源と断定はできない。
- 画面・データへの影響および通知の真因は未特定。HTTP 200 をもって復旧済みとは判断しない。
- 次回の定期処理から、削除対象の処理件数・成功件数・失敗件数だけを安全な集計ログとして残す。通知の有無と実際の削除処理を照合し、対象IDや画像パスは記録しない。

## 2026-09-24 00:00 JST の公開版確認

- 最終デプロイ `dpl_66M93zhWrJZFEhuD9ukt34fRataK` は Ready で、本番ドメインのエイリアスも付いている。
- 00:00:40 JST の自然実行でも `/api/cron/persona-purge` は HTTP 200 だったが、同じ指紋 `ef4886c13c74d60de2017d0b` の通知が1件発生した。通知の照合IDは `03a136fd-d023-43d4-8561-3b9914103cbd`。
- 追加した `[persona-purge] result` の集計行は、該当時刻の Vercel CLI のログ取得では確認できなかった。ログの欠落理由と実際の処理件数は未確認であり、HTTP 200 や通知が1件に収まったことから影響なし・復旧済みとは判断しない。

## 00:15 JST の再発と診断ログの補修

- 00:15:40 JSTにも `/api/cron/persona-purge` から同じ指紋の通知が1件発生し、リクエストはHTTP 200だった。通知が継続しているため、原因は未解明。
- `console.log` による処理件数は0:00、0:15ともにVercel CLIから取得できなかった。同じリクエストの `console.warn` は取得できるため、個人情報を含まない `processed`・`completed`・`failed` の集計行だけを警告レベルで出すよう変更。次の自然実行で取得できるか確認する。

## 00:30 JST の自然実行

- デプロイ `dpl_DK8wbo3MbhAXQTSNbLHVbg1FrzuR` の `/api/cron/persona-purge` は HTTP 200。新しい集計ログを取得でき、`processed: 0, completed: 0, failed: 0` だった。
- 同じリクエストのログには従来の指紋の通知を確認できなかった。ただし、この1回の不再発は過去の通知の原因特定や復旧の証明にはならない。次回以降の自然実行も確認する。

## 00:45–01:15 JST の追加確認

- 00:45、01:00、01:15の自然実行にも `[persona-purge] result` が残り、いずれも `processed: 0, completed: 0, failed: 0` だった。少なくともこの時間帯には削除対象を処理していない。
- 対象処理が0件なので、削除対象がある場合の正常性はまだ検証できない。元の通知の発生原因も未特定であり、復旧済みとは判断しない。
