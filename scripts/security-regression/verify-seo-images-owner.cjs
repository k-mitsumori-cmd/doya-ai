const assert=require('node:assert/strict'),{z}=require('zod');const {load,check,results}=require('./load-typescript.cjs');
(async()=>{for(const route of ['banner','diagram','batch','suggest'])for(const identity of ['owner','other','guest','other-guest','anonymous'])for(const kind of ['user','guest'])await check(route+' '+identity+' '+kind,async()=>{
const row={id:'article',userId:kind==='user'?'u':null,guestId:'g',title:'title',finalMarkdown:'## Heading\nbody',keywords:[]};let ai=0,writes=0,storage=0;
const owner=load('src/lib/seoArticleOwner.ts',{'next-auth':{getServerSession:async()=>['owner','other'].includes(identity)?{user:{id:identity==='owner'?'u':'x'}}:null},'@/lib/auth':{},'@/lib/seoAccess':{getGuestIdFromRequest:()=>identity==='guest'?'g':identity==='other-guest'?'x':null}});
const api=load('src/app/api/seo/articles/[id]/images/'+route+'/route.ts',{'next/server':{NextResponse:Response},zod:{z},'@/lib/seoArticleOwner':owner,'@seo/lib/bootstrap':{ensureSeoSchema:async()=>{}},'@seo/lib/bannerPlan':{guessArticleGenreJa:()=>'',pickRandomPatterns:()=>[{label:'pattern'}],buildBannerPromptFromPattern:()=>''},'@seo/lib/storage':{ensureSeoStorage:async()=>{},saveBase64ToFile:async()=>{storage++;return{relativePath:'synthetic.png'}}},'@seo/lib/gemini':{geminiGenerateImagePng:async()=>{ai++;return{dataBase64:'AA=='}},geminiGenerateJson:async()=>{ai++;return{diagrams:[]}}},'@/lib/prisma':{prisma:{seoArticle:{findFirst:async({where})=>Object.entries(where).every(([k,v])=>row[k]===v)?row:null},seoImage:{create:async()=>{writes++;return{id:'image'}}}}}},{setTimeout:fn=>fn()});
const res=await api.POST({json:async()=>({title:'concept',description:'detail',diagrams:[{title:'concept',description:'detail'}]})},{params:Promise.resolve({id:'article'})});const allowed=identity==='owner'&&kind==='user'||identity==='guest'&&kind==='guest';assert.equal(res.status,allowed?200:identity==='anonymous'?401:404);assert.equal(ai,allowed?1:0);assert.equal(writes,allowed&&route!=='suggest'?1:0);assert.equal(storage,writes);
});console.log(JSON.stringify({passed:results.length,results},null,2));})().catch(e=>{console.error(e);process.exitCode=1});
